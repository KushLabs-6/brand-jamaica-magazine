{
  "name": "brand-jamaica-magazine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/generate-metadata.cjs && vite",
    "build": "node scripts/generate-metadata.cjs && vite build",
    "preview": "vite preview",
    "generate": "node scripts/generate-metadata.cjs"
  },
  "devDependencies": {
    "vite": "^8.0.4"
  },
  "dependencies": {
    "multer": "^2.1.1",
    "page-flip": "^2.0.7"
  }
}
